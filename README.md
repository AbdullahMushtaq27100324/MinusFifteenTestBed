To run this code:

1. Clone the repository
2. Type into the terminal: go mod tidy
3. Run docker and then type in a new terminal docker run -it -p 1883:1883 eclipse-mosquitto 
4. Type in a new terminal: go run .
5. Open up your browser and type into the search bar: "http://localhost:8080"
